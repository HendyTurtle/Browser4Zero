# Environment variables

# OpenAI Compatible API
# Fill it with your own config
OPENAI_API_KEY=sk-blablabla
OPENAI_MODEL=deepseek/deepseek-v3.2
OPENAI_API_BASE=https://openrouter.ai/api/v1

# If this is 'true' you won't see the browser, but it may be more likely to be detected by websites. 
# False for better compatibility and observability, but it will open a visible browser window.
BROWSER_HEADLESS=false

# Leave these as they are by default, or adjust them if you know what you're doing
PAGE_READY_TIMEOUT=30
SCREENSHOT_WIDTH=1280
NAVIGATION_TIMEOUT=15000
LLM_TIMEOUT=60

# Enable vision (screenshot) mode. Sends screenshots to the LLM along with element lists.
# Improves accuracy on visually complex pages, but costs more tokens.
# For most tasks, vision mode is not necessary and may even reduce performance, so it's disabled by default.
# true = enabled, false = disabled (default)
VISION_ENABLED=false

# Max number of steps the agent will take before giving up.
# ! Be cautious with very high values to avoid infinite loops
MAX_STEPS=100